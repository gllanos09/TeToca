package com.tetocaApp.tetoca.ui.theme

import androidx.compose.ui.graphics.Color

// Primario — Esmeralda: identidad de marca, TopAppBar, acciones principales.
val Emerald80 = Color(0xFFA7F3D0)
val Emerald40 = Color(0xFF059669)

// Secundario / acento de marca — Teal (azul-petróleo): FAB y botones
// secundarios. Transmite tecnología/confianza sin invadir el significado
// de alerta que ya tiene el ámbar.
val Teal80 = Color(0xFF99D8E6)
val Teal40 = Color(0xFF0E7490)

// Semántico de alerta — Ámbar: reservado para "stock bajo" y advertencias.
// No se usa como color de acción genérico en la UI.
val Amber80 = Color(0xFFFDE68A)
val Amber40 = Color(0xFFD97706)

// Semántico crítico — Rojo: "stock crítico" (producto por debajo del mínimo).
val Red80 = Color(0xFFFCA5A5)
val Red40 = Color(0xFFDC2626)

// Neutros
val Slate90 = Color(0xFFE2E8F0)
val Slate20 = Color(0xFF1E293B)